/**class Solution {
    public int maxReturn(Queue<Integer> q)
    {
        Queue<Integer> q2=new LinkedList(q);
        int max=Integer.MIN_VALUE;
        while(!q2.isEmpty())
        { 
           max=Math.max(q2.remove(),max);
        }
        return max;
    }
    public int[] maxSlidingWindow(int[] nums, int k) {
        if (nums == null || nums.length == 0) 
        return new int[0];
        Queue<Integer> q=new LinkedList<>();
        int ar[]=new int[nums.length-k+1];
        for(int i=1;i<=k;i++)
        {
            q.add(nums[i-1]);
        }
            ar[0]=maxReturn(q);
            int j=1;
            for(int i=k;i<nums.length;i++)
            {
                q.remove();
                q.add(nums[i]);
                ar[j]=maxReturn(q);
                j++;
            }
        
        return ar;
    }
}*/
import java.util.ArrayDeque;
import java.util.Deque;

class Solution {
    public int[] maxSlidingWindow(int[] nums, int k) {
        if (nums == null || nums.length == 0) 
        return new int[0];
        
        int n = nums.length;
        int[] ar = new int[n - k + 1];
        int j = 0; 
        
        
        Deque<Integer> dq = new ArrayDeque<>();
        
        for (int i = 0; i < n; i++) {
            if (!dq.isEmpty() && dq.peekFirst() < i - k + 1) {
                dq.pollFirst();
            }
            while (!dq.isEmpty() && nums[dq.peekLast()] < nums[i]) {
                dq.pollLast();
            }
            
            
            dq.offerLast(i);
            
            
            if (i >= k - 1) {
                ar[j] = nums[dq.peekFirst()];
                j++;
            }
        }
        
        return ar;
    }
}